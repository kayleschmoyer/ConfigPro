# Local defaults
HOST=localhost
PORT=5173

# When running on a VM / public IP
# Set PUBLIC=1 to bind 0.0.0.0 and accept remote connections
# Optionally set HMR_HOST to your VM IP or domain for hot reloads from another device
# PUBLIC=1
# HOST=0.0.0.0
# HMR_HOST=123.45.67.89
